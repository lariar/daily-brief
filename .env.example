# Google Calendar OAuth 2.0 Credentials
# Get these from Google Cloud Console OAuth 2.0 Client
GOOGLE_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret

# Personal account refresh token (get from oauth-setup.js script)
GOOGLE_PERSONAL_REFRESH_TOKEN=your-personal-refresh-token

# Work account refresh token (get from oauth-setup.js script)
GOOGLE_WORK_REFRESH_TOKEN=your-work-refresh-token

# Todoist API Token
# Get from: https://todoist.com/prefs/integrations
TODOIST_API_TOKEN=your_todoist_api_token_here

# Optional: Specific calendar IDs (defaults to 'primary')
PERSONAL_CALENDAR_ID=primary
WORK_CALENDAR_ID=primary

# Optional: Email settings (for future email functionality)
# EMAIL_SERVICE=gmail
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password

# Optional: Slack webhook (for future Slack integration)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# Environment
NODE_ENV=development

# Timezone (optional - defaults to system timezone)
# Examples: America/New_York, America/Los_Angeles, Europe/London, etc.
# TIMEZONE=America/New_York